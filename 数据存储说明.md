# 📦 数据存储说明

## 🎯 新增功能：本地数据持久化

### v2.1 更新内容

系统现在会自动将所有会话数据和上传文件保存到本地文件夹，即使服务器重启也不会丢失数据！

---

## 📂 目录结构

```
项目根目录/
├── data/                           # 会话数据存储
│   ├── session_id_1/              # 会话1的所有数据
│   │   ├── session.json          # 会话详细信息
│   │   ├── 科技公司A_银行流水.pdf
│   │   ├── 科技公司A_承诺书.pdf
│   │   ├── 企业B_银行流水.pdf
│   │   └── 企业B_承诺书.pdf
│   ├── session_id_2/              # 会话2的所有数据
│   │   ├── session.json
│   │   ├── 公司C_银行流水.pdf
│   │   ├── 公司C_承诺书.pdf
│   │   ├── 公司D_银行流水.pdf
│   │   └── 公司D_承诺书.pdf
│   └── ...
│
├── uploads/                        # 临时上传目录（现在为空）
├── 备份-v2.0-20251128/            # 代码备份
└── ...
```

---

## 💾 数据持久化机制

### 1. 会话数据存储

**存储位置：** `data/{session_id}/session.json`

**存储时机：**
- ✅ 创建会话时
- ✅ 加入会话时
- ✅ 上传文件后
- ✅ 提交流水金额后
- ✅ 查看比较结果后

**存储内容：**
```json
{
  "session_id": "abc123...",
  "created_at": "2025-11-28T20:00:00",
  "status": "revealed",
  "company_a": {
    "name": "科技公司A",
    "committed": true,
    "level": 2,
    "level_info": {
      "level": 2,
      "name": "成长级",
      "description": "100万-1000万"
    },
    "bank_statement": "文件名.pdf",
    "commitment_letter": "承诺书.pdf",
    "files_uploaded": true
  },
  "company_b": {
    "name": "企业B",
    "committed": true,
    "level": 3,
    "level_info": {
      "level": 3,
      "name": "发展级",
      "description": "1000万-1亿"
    },
    "bank_statement": "文件名.pdf",
    "commitment_letter": "承诺书.pdf",
    "files_uploaded": true
  },
  "result": {
    "comparison": "lower",
    "message": "企业B的流水档次更高",
    "company_a": {...},
    "company_b": {...}
  }
}
```

### 2. 文件存储

**存储位置：** `data/{session_id}/`（与session.json在同一目录）

**文件命名规则：**
```
{公司名称}_{文件类型}.{扩展名}

示例：
科技公司A_银行流水.pdf
科技公司A_承诺书.pdf
企业B_银行流水.pdf
企业B_承诺书.pdf
```

**优点：**
- ✅ 所有会话相关文件集中在一个文件夹
- ✅ 文件名清晰易读，直接显示公司名
- ✅ 便于整体备份和管理
- ✅ 删除会话时只需删除一个文件夹

---

## 🔄 自动恢复机制

### 服务器重启后的行为

**1. 启动时自动加载历史会话**
```python
# 服务器启动时执行
load_all_sessions()
# ✅ 已加载 N 个历史会话
```

**2. 会话状态恢复**
- 所有未完成的会话可以继续
- 已完成的会话可以查看历史
- 文件关联关系保持完整

**3. 用户体验**
- 用户刷新页面后可以恢复会话
- 不会因服务器重启丢失数据
- 历史记录可追溯

---

## 📊 数据管理API

### 1. 查看历史记录

**接口：** `GET /api/history`

**返回：**
```json
{
  "success": true,
  "count": 5,
  "history": [
    {
      "session_id": "abc123...",
      "created_at": "2025-11-28T20:00:00",
      "company_a_name": "科技公司A",
      "company_b_name": "企业B",
      "result": {
        "comparison": "lower",
        "message": "企业B的流水档次更高"
      }
    },
    ...
  ]
}
```

**使用示例：**
```bash
curl http://localhost:5001/api/history
```

### 2. 导出会话数据

**接口：** `GET /api/export/{session_id}`

**返回：**
```json
{
  "success": true,
  "data": {
    "session_id": "abc123...",
    "created_at": "2025-11-28T20:00:00",
    "status": "revealed",
    "company_a": {
      "name": "科技公司A",
      "level": {...},
      "files": {
        "bank_statement": "文件名.pdf",
        "commitment_letter": "承诺书.pdf"
      }
    },
    "company_b": {...},
    "result": {...}
  }
}
```

**使用示例：**
```bash
curl http://localhost:5001/api/export/abc123...
```

---

## 🛠️ 数据管理操作

### 查看所有会话

```bash
# 进入data目录
cd data

# 列出所有会话
ls -l

# 查看特定会话
cat abc123.../session.json | python -m json.tool
```

### 查看会话的所有文件

```bash
# 查看某个会话的所有内容
ls -lh data/abc123.../

# 应该看到：
# session.json
# 公司A_银行流水.pdf
# 公司A_承诺书.pdf
# 公司B_银行流水.pdf
# 公司B_承诺书.pdf

# 查看所有会话的文件
find data/ -name "*.pdf"

# 按大小排序查看
find data/ -name "*.pdf" -exec ls -lh {} \;
```

### 统计数据

```bash
# 会话总数
ls -d data/*/ | wc -l

# 文件总数（不包括session.json）
find data/ -name "*.pdf" | wc -l

# 所有文件总大小
du -sh data/

# 各类文件统计
find data/ -name "*银行流水*" | wc -l    # 银行流水
find data/ -name "*承诺书*" | wc -l      # 承诺书

# 查看最大的文件
find data/ -name "*.pdf" -exec du -h {} \; | sort -hr | head -10
```

### 清理过期数据

```bash
# 删除30天前的会话（慎用！包含所有文件）
find data/ -maxdepth 1 -type d -mtime +30 -exec rm -rf {} \;

# 清理特定会话（删除整个文件夹）
rm -rf data/abc123...

# 清理所有会话，只保留最近10个
cd data
ls -t | tail -n +11 | xargs rm -rf

# 备份后清理
tar -czf backup_$(date +%Y%m%d).tar.gz data/
rm -rf data/old_session_id
```

---

## 🔒 数据安全

### 1. 文件权限

**建议设置：**
```bash
# data目录只有所有者可读写
chmod 700 data/

# uploads目录只有所有者可读写
chmod 700 uploads/

# 会话文件只有所有者可读写
chmod 600 data/*/session.json

# 上传文件只有所有者可读
chmod 600 uploads/*
```

### 2. 敏感信息

**会话数据中不包含：**
- ❌ 具体流水金额（只存储档次）
- ❌ 密钥和随机数（secret）
- ❌ 用户密码或认证信息

**会话数据包含：**
- ✅ 公司名称
- ✅ 档次等级
- ✅ 文件引用（文件名）
- ✅ 比较结果

### 3. 定期备份

```bash
# 创建备份脚本
cat > backup_data.sh << 'EOF'
#!/bin/bash
DATE=$(date +%Y%m%d_%H%M%S)
BACKUP_NAME="数据备份_$DATE"

# 创建备份（data目录已包含所有文件）
tar -czf "$BACKUP_NAME.tar.gz" data/

# 显示备份大小
SIZE=$(du -h "$BACKUP_NAME.tar.gz" | cut -f1)
COUNT=$(find data/ -name "*.json" | wc -l)

echo "✅ 备份完成: $BACKUP_NAME.tar.gz"
echo "   大小: $SIZE"
echo "   会话数: $COUNT"
EOF

chmod +x backup_data.sh

# 运行备份
./backup_data.sh

# 恢复备份
tar -xzf 数据备份_20251128.tar.gz
```

---

## 📈 数据分析

### 1. 会话统计

```python
# 在Python中分析
import json
import os
from collections import Counter

# 统计档次分布
levels = []
for session_id in os.listdir('data'):
    session_file = os.path.join('data', session_id, 'session.json')
    if os.path.exists(session_file):
        with open(session_file) as f:
            data = json.load(f)
            if data.get('company_a', {}).get('level'):
                levels.append(data['company_a']['level'])
            if data.get('company_b', {}).get('level'):
                levels.append(data['company_b']['level'])

print("档次分布：")
for level, count in Counter(levels).items():
    print(f"  Level {level}: {count} 家公司")
```

### 2. 导出报表

```python
# 生成CSV报表
import csv

def export_to_csv(output_file='sessions_report.csv'):
    with open(output_file, 'w', newline='', encoding='utf-8') as f:
        writer = csv.writer(f)
        writer.writerow(['会话ID', '创建时间', '公司A', '公司B', '档次A', '档次B', '结果'])
        
        for session_id in os.listdir('data'):
            session_file = os.path.join('data', session_id, 'session.json')
            if os.path.exists(session_file):
                with open(session_file) as sf:
                    data = json.load(sf)
                    writer.writerow([
                        session_id,
                        data.get('created_at'),
                        data['company_a'].get('name'),
                        data['company_b'].get('name'),
                        data['company_a'].get('level'),
                        data['company_b'].get('level'),
                        data.get('result', {}).get('message')
                    ])
    
    print(f"✅ 报表已导出: {output_file}")

export_to_csv()
```

---

## ⚠️ 注意事项

### 1. 磁盘空间

**估算：**
- 每个会话：~10-20MB（包含文件）
- 100个会话：~1-2GB
- 建议预留：5GB以上

**监控：**
```bash
# 查看磁盘使用
df -h .

# 查看项目大小
du -sh data/ uploads/
```

### 2. Git管理

**重要：** `data/` 和 `uploads/` 已添加到 `.gitignore`

- ✅ 不会提交到Git
- ✅ 保护用户隐私
- ✅ 避免仓库过大

**如需备份：**
```bash
# 使用其他方式备份数据
rsync -av data/ /path/to/backup/data/
rsync -av uploads/ /path/to/backup/uploads/
```

### 3. 生产环境

**建议改进：**
- 使用专业数据库（PostgreSQL、MongoDB）
- 文件存储使用OSS（阿里云、腾讯云）
- 添加数据加密
- 实现自动清理机制
- 设置访问权限控制

---

## 🔍 故障排查

### 问题1：启动时提示加载会话失败

**检查：**
```bash
# 检查data目录是否存在
ls -ld data/

# 检查权限
ls -l data/

# 检查JSON文件格式
python -m json.tool data/xxx/session.json
```

**解决：**
```bash
# 重新创建目录
mkdir -p data/

# 修复权限
chmod 700 data/
```

### 问题2：文件找不到

**检查：**
```bash
# 确认文件存在
ls uploads/

# 检查会话中的文件名
cat data/xxx/session.json | grep -E "bank_statement|commitment_letter"
```

### 问题3：数据占用过多空间

**清理：**
```bash
# 只保留最近30天的数据
find data/ -type f -mtime +30 -delete
find uploads/ -type f -mtime +30 -delete

# 压缩旧数据
find uploads/ -type f -name "*.pdf" -mtime +7 -exec gzip {} \;
```

---

## 📚 相关文档

- **README.md** - 项目主文档
- **文件上传功能说明.md** - 文件上传详解
- **v2.0升级指南.md** - 版本升级说明
- **项目协作指南.md** - 团队协作

---

**版本：** v2.1  
**更新日期：** 2025-11-28  
**状态：** ✅ 生产可用

**数据现在安全持久化到本地了！** 🎉

